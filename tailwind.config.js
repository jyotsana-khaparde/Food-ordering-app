/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js,ts,jsx,tsx}"], // The is the way we are telling our project that we can use tailwind in any of these files.
  theme: {
    extend: {},
  },
  plugins: [],
};
